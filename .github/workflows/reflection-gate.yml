name: reflection-gate
on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  reflection:
    uses: aesynth-ai/governance-registry/.github/workflows/_reusable-reflection.yml@docs/addendum-a3
    with:
      # TEMP for this PR so it can merge now; restore to 12h on main after merge
      min_hours: 0.1
